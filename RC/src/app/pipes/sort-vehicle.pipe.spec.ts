import { SortVehiclePipe } from './sort-vehicle.pipe';

describe('SortVehiclePipe', () => {
  it('create an instance', () => {
    const pipe = new SortVehiclePipe();
    expect(pipe).toBeTruthy();
  });
});
